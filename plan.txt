1. Modify `omlx/optimization/manager.py` -> `PassManager.get_execution_order(self, stage: Optional[CompilerStage] = None)`
   - Filter passes by the given stage before returning the ordered list.

2. Introduce Analysis Result Framework in `omlx/optimization/passes.py` or new file `omlx/optimization/analysis.py`.
   - Add classes for Analysis results (e.g. `AnalysisResult`, `DependencyAnalysisResult`, etc.)
   - Update `AnalysisPass.analyze` to return `AnalysisResult` instead of `None`.
   - Add Analysis Cache integration in `OptimizationContext` or `PassManager` to reuse analysis results.

3. Introduce Optimization Pipeline Phases in `omlx/optimization/pipeline.py`.
   - Define phases: `Canonicalization`, `Analysis`, `Simplification`, `Optimization`, `Validation`, `Backend Preparation`.
   - Validate phase ordering during pass execution.

4. Add support for Parallel Analysis.
   - Use `concurrent.futures.ThreadPoolExecutor` in `OptimizationPipeline.execute()` for `AnalysisPass`es that can run in parallel without conflicts.
   - Ensure thread-safety by not sharing mutable state.

5. Enhance Optimization Validation in `omlx/optimization/validation.py`.
   - Ensure `OptimizationPass`es don't mutate input artifacts unexpectedly (use deepcopy/immutability checks if possible).

6. Expand Statistics & Diagnostics in `omlx/optimization/statistics.py` & `diagnostics.py`.
   - Add fields for cache utilization, analysis reuse, parallel execution stats.

7. Tests & Docs.
   - Update `tests/test_optimization.py` with stage-aware scheduling, analysis reuse, etc.
   - Write docs as requested.
