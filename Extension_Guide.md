# Extension Guide

## Extending the Framework
1. Create new `MetadataNormalizer` mapping.
2. Update `ModelClassifier` for novel architectures.
3. Update `CapabilityExtractor` for new hardware capabilities.
