# Accessibility Report

- Contrast ratios for existing dark/light mode must be maintained with new Surface tokens.
- Focus rings (`focus:ring-neutral-900`) need to migrate to interactive tokens.
- ARIA labels and semantic HTML generally intact but will be verified post-migration.
