# Remaining Legacy UI Report

- All files in `omlx/admin/templates/` currently use legacy Tailwind utility colors instead of the new tokens.
- Legacy CSS in `dashboard.css` hardcodes colors for elements like range sliders and toggle switches.
