# Design Token Adoption Report

- Current: `dashboard.css` and `tailwind.config.js` use custom `--bg-primary` but with generic names.
- Target: Implement `Surface 0-4` and `Accent (Execution, Runtime, Planning, Success, Warning, Error)`.
- Typography tokens (Display, Title, Heading, Body, Caption, Code) need to be configured in Tailwind.
