# Branding Migration Report

- 'oMLX' text found in i18n files, page titles, and headers.
- Favicon: `/admin/static/favicon.svg`
- Logos: `AppLogo.imageset` in mac app (out of scope if not web?).
- 'omlx' URL paths and local storage keys (e.g. `omlx-chat-theme`). Local storage should be migrated to `one-chat-theme`.
