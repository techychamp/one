name: 'Setup Swift & Xcode'
description: 'Selects the default Xcode and CLI tools'
runs:
  using: 'composite'
  steps:
    - name: Select Xcode
      shell: bash
      run: |
        sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
        xcodebuild -version
        swift --version
