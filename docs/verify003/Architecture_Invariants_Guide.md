# Architecture Invariants Guide

Declares that RuntimeCompilerService exclusively orchestrates compilation, the engine simply executes schedules, and the BackendOperationGraph isolates backend complexity.