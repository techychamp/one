# Recommendations for BACKEND-003
In the next milestone, integrate the scoring framework into the `ExecutionPlanner`'s Cost Model. Begin migrating hardcoded runtime backend selection logic to rely on the generic scores generated by the `BackendScoringFramework`.
