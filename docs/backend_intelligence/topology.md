# Hardware Topology Guide
`HardwareTopology` abstracts the layout and connection types of the backend execution environment (e.g., Single Device, Unified Memory, NUMA, Distributed). This allows the planner to reason about data transfer and routing costs purely from metadata.
