# Future Runtime Integration Notes
The Execution Planner will consume `BackendScoringFramework` and `BackendDescriptor`s from the `AdapterRegistry` to evaluate execution graphs against constraints before passing the graph to the execution engine.
