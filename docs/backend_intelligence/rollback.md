# Rollback Procedure
If regressions occur, revert the commit associated with BACKEND-002. Since no runtime code depends on the new intelligence fields yet, a rollback will not affect active inference paths.
