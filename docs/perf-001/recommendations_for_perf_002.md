# Recommendations for PERF-002

- Implement serialized on-disk caching using SQLite for cross-process capability persistence.
- Implement memory footprint estimators for compiled graphs.
- Optimize the `CacheKey` JSON serialization overhead for extremely complex objects.
