# Runtime Integration Report
ExecutionEngine consumes RuntimeSession directly. It extracts the ExecutionContext and executes the contained graphs. The engine never bypasses RuntimeSession and never constructs execution contexts itself.
# User Review Required
