# Compatibility Guide

## Checking Compatibility
Models are validated against standard metadata requirements. The framework prevents models with unsupported parameters from advancing by reporting diagnostic errors.
